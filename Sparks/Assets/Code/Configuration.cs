

// Holder for global application configurations
// Eventually these could be loaded/saved off the local storage
// allowing users to "configure" there installation


public static class Configuration {

    // The speed at which the life point change animation should progress
    public static float LP_LABEL_ANIMATION_SPEED = 50.0f;

    // What should each "player" start with, in terms of life points
    public static int DEFAULT_STARTING_LP = 8000;
}
